version: '3.9'
services:
  api:
    image: python:3.11-slim
    working_dir: /workspace
    volumes:
      - ./:/workspace
    command: bash -lc "pip install -e .[dev] && uvicorn app.main:app --host 0.0.0.0 --port 8000"
    ports:
      - "8000:8000"
